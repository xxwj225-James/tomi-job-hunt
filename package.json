{
  "name": "tomi-job-hunt",
  "version": "0.1.0",
  "private": true,
  "description": "AI job-hunting assistant: local-first JD matching, resume tailoring and pitch generation for the Chinese job market",
  "license": "MIT",
  "workspaces": [
    "core",
    "extension"
  ],
  "scripts": {
    "dev": "npm run dev -w core",
    "build": "npm run build -w core && npm run build -w extension",
    "test": "npm test -w core && npm test -w extension"
  },
  "engines": {
    "node": ">=20"
  }
}
